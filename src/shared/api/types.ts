export interface ResponseFetchGetEventsList {
  timestamp: number
  duration: number
  zone: {
    left: number
    top: number
    width: number
    height: number
  }
}

// Request interfaces
export interface RequestFetchGetEventsList {}
